cors:
  allowed-origins: "*"
  allowed-methods: "*"
  allowed-headers: "*"
  exposed-headers: "*"

jwt:
  secret-key: "366cbea5d1cf0087125450504c5a7a68bd610d4925fe9fe9258006d66de8b351"
  expire-after: "86400000"

management:
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    tags:
      application: "IssueCrush"

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/issuecrush
    username: jd
    password: admin4049
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect